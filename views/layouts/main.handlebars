<!DOCTYPE html>
  <html>
    <head>
        <meta charset="utf-8">
        {{#if isFr}}
            <title>{{translation.fr.pageTitle}}</title>
        {{/if}}
        {{#if isEn}}
            <title>{{translation.en.pageTitle}}</title>
        {{/if}}
        {{#if isEs}}
            <title>{{translation.es.pageTitle}}</title>
        {{/if}}
        {{!-- <title>Awesome Handlebars</title> --}}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    </head>
    <body>

        {{!-- body va chercher la view passée en parametre du render --}}
        {{{body}}}

    </body>
</html>